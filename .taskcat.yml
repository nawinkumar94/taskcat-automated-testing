project:
  name: taskcat-example
  regions:
    - us-east-1
tests:
  taskcat-test:
       parameters:
           KeyName: 'ansible-course-keypair'
           NameOfService: 'taskcat-instance'
       template: ./sqs.yml
                          
                          
